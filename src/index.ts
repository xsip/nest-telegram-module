import { TELEGRAM_BOT_TOKEN, TelegramModule } from './telegram.module';
import { OnTelegramMessage, TelegramService } from './telegram.decorator';
export {
  TelegramModule,
  OnTelegramMessage,
  TelegramService,
  TELEGRAM_BOT_TOKEN,
};
